# Mechanistic interpretability playground.
# Install into a fresh virtualenv:
#     python3 -m venv .venv
#     .venv/bin/pip install -r requirements.txt
# Then generate the data and run the app:
#     .venv/bin/python export_graph.py   # writes graph_data.json
#     .venv/bin/python app.py            # serves the visualizer + chat at :5050

transformer-lens==3.4.0   # the microscope: run_with_cache + named hook points (export_graph.py)
torch==2.7.1              # pulled in by transformer-lens; pinned for reproducibility

# For app.py — the local visualizer + Claude chat server:
anthropic>=0.112.0        # Claude API SDK (the /chat endpoint, model claude-opus-4-8)
flask>=3.1.0              # serves the page + proxies chat to Claude (keeps your key server-side)
